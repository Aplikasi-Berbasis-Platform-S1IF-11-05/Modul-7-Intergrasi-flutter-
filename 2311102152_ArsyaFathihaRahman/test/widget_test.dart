import 'package:flutter_test/flutter_test.dart';

void main() {
  testWidgets('MagangHub app smoke test', (WidgetTester tester) async {
    // Firebase needs to be initialized for this app to run.
    // Integration testing should be done on a real device.
    expect(true, isTrue);
  });
}
